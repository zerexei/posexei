[project]
name = "social-post-service"
version = "0.1.0"
description = "Social post service"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.136.0",
    "shared",
]

[tool.uv.sources]
shared = { path = "../../services/shared" }
