[project]
name = "social-publish-service"
version = "0.1.0"
description = "Social publish service"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.136.0",
    "httpx>=0.28.1",
    "shared",
]

[tool.uv.sources]
shared = { path = "../../services/shared" }
