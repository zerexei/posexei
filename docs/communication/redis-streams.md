# Redis Streams Topology

Documents the Redis Streams topology used for asynchronous communication.
Explains consumer groups, stream naming conventions (`jobs:{service}:inbound`), `XADD` limits, and `XREADGROUP` consumption strategies.
