# Event Schema and Payload

Provides the schema and payload structure for all system events.
Standardizes the metadata format (correlation IDs, timestamps, event types) required for tracking jobs across service boundaries.
