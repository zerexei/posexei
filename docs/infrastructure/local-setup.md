# Local Setup Guide

Instructions for spinning up the complete system locally via Docker Compose.
Covers environment variable overrides, volume mounts for PostgreSQL/Redis, and hot-reloading for FastAPI services.
